# 2026-04-08 Debugging Session

## Telegram TTS [[tts]] Bug Investigation

### Problem
- `/tts audio` command works correctly (sends voice note in Telegram)
- `[[tts]]` tag in normal assistant replies appears as literal text in Telegram instead of triggering TTS audio

### Confirmed Facts
- TTS base generation works: `/tts audio` → `textToSpeech(...)` → voice message delivered
- `maybeApplyTtsToPayload(...)` works in isolated test: returns cleaned text + `mediaUrl` + `audioAsVoice: true`
- `parseTtsDirectives(text, policy, options)` returns text unchanged if `!policy.enabled` — matches the symptom
- Session Telegram: `agent:main:telegram:direct:187220721`, `ttsAuto = undefined`
- Config: `messages.tts.auto = "tagged"`, `messages.tts.mode = "final"`, `messages.tts.provider = "microsoft"`
- `resolveEffectiveTtsAutoState(...)` falls to config when `sessionAuto` is `undefined` → should resolve to `"tagged"`
- No `~/.openclaw/settings/tts.json` exists
- `journalctl` shows only `sendMessage ok`, never `sendVoice`/`sendAudio` — TTS media never reaches outbound

### Key Code Files (in `/home/omi/.npm-global/lib/node_modules/openclaw/dist/`)
- `dispatch-BvwlsDx2.js` — main dispatch, calls `maybeApplyTtsToReplyPayload(...)` at lines ~413, 596, 650, 704
- `dispatch-acp-Cu9dm2kp.js` — ACP dispatch with own `deliver()` at line ~568, uses `maybeApplyAcpTts(...)` (different function!)
- `extensions/speech-core/runtime-api.js` — defines `maybeApplyTtsToPayload(...)`
- `provider-error-utils-Z09hDOl0.js` — defines `parseTtsDirectives(...)`
- `outbound-D8YI0oq5.js` — `sendVoice(...)` / `sendVoiceFromLocal(...)` for Telegram
- `bot-DMS2hpfD.js` — bot entry point, uses `dispatchReplyWithBufferedBlockDispatcher`
- `provider-dispatcher-COjV0JaB.js` — thin wrapper, calls `dispatchInboundMessageWithBufferedDispatcher`

### Hypothesis (still being validated)
Two possible routes for delivery:
1. **Main dispatch** (`dispatch-BvwlsDx2.js`): calls `maybeApplyTtsToReplyPayload(...)` — confirmed working in isolation
2. **ACP dispatch** (`dispatch-acp-Cu9dm2kp.js`): uses `maybeApplyAcpTts(...)` — possibly a simplified version that doesn't process `[[tts]]`

The bot uses `dispatchReplyWithBufferedBlockDispatcher` → main dispatch path. Need to verify:
- Whether Telegram actually goes through main dispatch or ACP
- Whether `maybeApplyAcpTts(...)` is the function actually being called for Telegram
- Where the `policy.enabled = false` condition is being hit (since `parseTtsDirectives` returns raw text when disabled)
- Why `journalctl` never shows `sendVoice` — indicating TTS media is never generated in the real path

### Next Steps
1. Inspect `maybeApplyAcpTts(...)` implementation — does it process `[[tts]]` or pass through?
2. Trace which dispatch route Telegram actually uses at runtime
3. Find where `policy.enabled` becomes `false` in the real execution path
4. Apply minimal patch
5. Verify with directed test in Telegram
