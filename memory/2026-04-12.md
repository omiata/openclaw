- El usuario quiere que, cuando una categoría sea de tipo lista, se muestre siempre en formato lista.
- Si la lista mezcla películas y series, prefiere que se separen en secciones distintas.
- El usuario quiere que, cuando una categoría sea de tipo lista, se muestre siempre en formato lista.
- Si la lista mezcla películas y series, prefiere que se separen en secciones distintas.
- En el trabajo del carrusel de Telegram para la skill bitácora, se confirmó que el problema de que “no salga” no era el runtime parcheado sino que la skill no estaba invocando `read_entries.py --telegram-carousel`; se actualizó `skills/bitacora/SKILL.md` para que en Telegram, al listar varias referencias enlazables, use `--telegram-carousel --carousel-cache-key` y devuelva el JSON crudo.
- Para validar ese carrusel hay que probarlo en Telegram real, no en webchat/openclaw-tui, porque esa superficie no renderiza el carrusel inline de Telegram.
- El fallo de que Telegram mostrara el JSON del carrusel como texto visible se debía a la ruta de entrega en `/home/omi/.npm-global/lib/node_modules/openclaw/dist/channel-ClQq9jsV.js`: el payload salía por la ruta de texto chunked antes de llegar al parser del carrusel. Se parcheó ese runtime para detectar texto JSON con `kind: "telegram_inline_carousel"` y normalizarlo como payload estructurado de Telegram, forzando el uso de `sendPayload` en vez de fragmentarlo como texto.
